import {operateDom} from './util';

/*
 * @params options {Object} 必须
*/
function Alert (params) {
  var defaults = {
    title: '',
    content: '',
    btns: [],
    zIndex: 100,
    // type: "android"
  };
  
}
export default Alert;